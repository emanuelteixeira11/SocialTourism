<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/filter_ranking"
        android:icon="@drawable/ic_filter"
        android:title="@string/action_settings"
        app:showAsAction="always">
        <menu>
            <item android:title="Order by distance"
                android:id="@+id/order_by_distance"/>
            <item android:title="@string/order_by_ranking"
                android:id="@+id/order_by_ranking"/>
            <item android:title="Show places open now"
                android:id="@+id/show_opened"
                android:checkable="true"/>
        </menu>
    </item>
</menu>
